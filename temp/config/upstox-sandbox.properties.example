# Sandbox Upstox credentials — copy to config/upstox-sandbox.properties (gitignored)
#
# Mirrors the Trade_J config/upstox-sandbox.properties.example template.
upstox.sandbox.clientId=
upstox.sandbox.clientSecret=
upstox.sandbox.redirectUri=http://127.0.0.1:18080/callback
upstox.sandbox.accessToken=
