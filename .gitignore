# Environment files
.env
.env.*
!.env.example

# Dhan secret files
config/dhan-pin.txt
config/dhan-totp-secret.txt

# Upstox secret files
config/upstox-pin.txt
config/upstox-totp-secret.txt

# Dhan credential properties (templates are committed; values are gitignored)
config/dhan-local.properties
config/dhan-sandbox.properties

# Upstox credential properties
config/upstox-live.properties
config/upstox-sandbox.properties

# ICICI credential properties
config/icici-local.properties
config/icici-pin.txt
config/icici-totp-secret.txt
config/icici-username.txt
config/icici-password.txt
config/icici-api-session.txt


__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/

# Phase 0 additions
.cache/
.runtime/
.env.local.sandbox
.env.local.live_readonly
.coverage.*
.mypy_cache/
.ruff_cache/
.DS_Store
*.egg-info/
build/
dist/

# Editor metadata
.qoder/
.trae/
.mimocode/
.import_linter_cache/

# Runtime data (created by the application)
market_data/
data/
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.duckdb
*.duckdb.wal

ZCode-3.1.1-mac-arm64.dmg

# Analytics materialized cache
analytics_cache/

# Data lake quarantine — corrupted / unparseable files
market_data/_quarantine/

# Archives and backups
archive/

graphify-out/
src/graphify-out/

# Runtime logs and lock files
*.log
runtime-dev/

# Process runtime state (top-level data only — NOT the Python `runtime` package)
/runtime/*.json
/runtime/*.lock
/runtime/*.log
/runtime/*.sqlite
/runtime/*.sqlite-shm
/runtime/*.sqlite-wal
/runtime/event-log/

# Process state under src/runtime package dir (locks, tokens, event logs)
src/runtime/*.json
src/runtime/*.lock
src/runtime/*.log
src/runtime/*.sqlite
src/runtime/*.sqlite-shm
src/runtime/*.sqlite-wal
src/runtime/event-log/

# Token state files that may be created inside src/brokers/* (should live in runtime/ instead)
src/brokers/runtime/*.json
src/brokers/runtime/*.lock
src/brokers/runtime/*.log
.hypothesis/
worktrees/
