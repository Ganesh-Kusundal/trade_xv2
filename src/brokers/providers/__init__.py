"""Concrete broker provider plugins (dhan / upstox / paper).

Replay is an analytics/runtime execution target (ADR-0012), not a broker
provider plugin — do not invent a stub ``providers/replay`` package.
"""
